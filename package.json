{
  "name": "ventealaferme",
  "description": "ventealaferme",
  "version": "1.0.0",
  "engines": {
    "yarn": "1.12.3"
  },
  "devDependencies": {
    "@symfony/webpack-encore": "^0.22.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "webpack-notifier": "^1.6.0"
  },
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev-server": "yarn encore dev-server",
    "dev": "yarn encore dev",
    "watch": "yarn encore dev --watch",
    "build": "yarn encore production --progress",
    "heroku-postbuild": "yarn encore production",
    "compile": [
      "composer require encore && yarn && yarn encore production"
    ]
  },
  "dependencies": {
    "common": "^0.2.5",
    "places.js": "^1.19.0",
    "leaflet": "^1.7.1",
    "jquery": "^3.6.1",
    "wow.js": "^1.2.2",
    "wowjs": "^1.1.3"
  }
}
